<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Устанавливаем уровень логирования для вашего логгера -->
    <logger name="BDD.StepsLogger" level="INFO"/>

    <!-- Устанавливаем уровень логирования для других библиотек -->
    <logger name="org.asynchttpclient" level="WARN"/>
    <logger name="com.codeborne.selenide" level="WARN"/>
    <logger name="org.openqa.selenium" level="WARN"/>
    <logger name="com.squareup.okhttp3" level="WARN"/>
    <logger name="org.aspectj" level="WARN"/>
    <logger name="org.apache.http" level="WARN"/>

    <!-- Корневой логгер -->
    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>